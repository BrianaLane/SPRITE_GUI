import numpy as np
import pandas as pd

#connect to FTDI
#initalize file to save data that gets overwritten
# for loop/ while loop reads the FTDI 
#pylibftdi is going to read in data
# write bytes to file
# counter to tracks number of lines 

#each line should have and index and when overwritten index still increases
#so index counter is only reset each time script is initialized